apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: slackfomo-embedded
  namespace: '{{repl if IsKurl}}{{repl ConfigOption "namespace" }}{{repl else}}{{repl Namespace}}{{repl end }}'
  annotations:
    kots.io/when: '{{repl and (ConfigOptionEquals "use_ingress" "1") IsKurl }}'
    cert-manager.io/cluster-issuer: letsencrypt-staging
    ingress.kubernetes.io/force-ssl-redirect: "true"
    kubernetes.io/ingress.class: contour
    kubernetes.io/tls-acme: "true"
spec:
  tls:
    - hosts:
        - repl{{ ConfigOption "ingress_hostname" }}
      secretName: slackfomo-embedded
  rules:
    - host: repl{{ ConfigOption "ingress_hostname" }}
      http:
        paths:
          - path: /
            backend:
              serviceName: slack-channel-created-fomo
              servicePort: 3000

